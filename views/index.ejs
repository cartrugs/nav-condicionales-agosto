<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>nav-condicionales</title>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/login">Login</a></li>
            <li><a href="/register">Register</a></li>
            <% if (showLinks === 'login' || showLinks === 'default') { %>
                <li><a href="/">Home</a></li>
            <% } %>
            <% if (showLinks === 'register' || showLinks === 'default') { %>
                <li><a href="/login">Login</a></li>
            <% } %>
            <% if (showLinks === 'register') { %>
                <li><a href="/register">Register</a></li>
            <% } %>
        </ul>
    </nav>
</body>
</html>
